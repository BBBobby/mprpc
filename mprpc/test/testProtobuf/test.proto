syntax = "proto3"; // 版本

package fixbug; // 声明代码所在的包，

message loginRequest{
	string name = 1;
	string pwd = 2;
}

message loginResonse{
	int32 errCode = 1;
	string errmsg = 2;
	bool success = 3;
}
